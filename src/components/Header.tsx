import { Logo } from "./Logo"

const Header = () => (
  <header className="w-full py-5 flex items-center justify-center bg-gray-700 border border-gray-600">
    <Logo />
  </header>
)

export default Header